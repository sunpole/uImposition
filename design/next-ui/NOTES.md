# Preview notes

The prototype is intentionally self-contained and has no external CSS, font or JavaScript dependencies.

This improves the chance of loading on old Android and iOS browsers and allows an exact commit preview through static raw HTML hosting.

Formal Chromium/device screenshot verification has not yet been run for this design-only checkpoint.
