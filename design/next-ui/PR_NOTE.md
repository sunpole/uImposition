# Draft PR note

This branch is intended to remain isolated until owner visual review.

Target: `main`

Expected PR title:

`Design: prototype the next operator UI system`

The PR must stay draft and must not merge until the owner accepts the visual direction.
