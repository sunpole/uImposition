# Owner acceptance feedback — 2026-07-31

## Positive result

The new operator-first application route is substantially better than the legacy technical page and the direction is accepted.

## Reported blockers and requested additions

- uniform compatibility error can remain visible for some time after all active rows are corrected;
- desktop top navigation should always expose every primary screen;
- back-side layout appears incorrect and should be verified as mirrored;
- add a combined face/back view while keeping separate views;
- remove a small unnecessary desktop vertical scroll at 1920×1080;
- widen the application closer to the screen edges;
- add batch product import from a text file and a downloadable template;
- expose calculation priorities: cost, forms, sheets, fewer mixed/empty layouts and other previously defined objectives;
- support different front/back color counts such as 4+1 and 1+4.

## Acceptance meaning

This feedback authorizes a blocker-only acceptance patch. It does not authorize root cutover or alpha.6 until the updated route is checked again by the owner.
